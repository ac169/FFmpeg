name: Clone
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run action
        uses: andrewthetechie/gha-clone-releases@v1
        with:
          token: ${{ secrets.SYNS_TOKEN }}
          src_repo: ac169/FFmpeg-Builds-Legacy
